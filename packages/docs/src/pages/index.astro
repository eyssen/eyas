---
// Root of the docs → default locale (respect trailing slash on BASE_URL).
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
return Astro.redirect(`${base}en/`);
---
