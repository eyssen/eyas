---
title: FAQ
description: Problemas frecuentes.
---

Puerto ocupado → `EYAS_PORT`. Sin UI → `build:web`. /docs 404 → `docs:build`. Auth proveedor → clave/CLI. Datos en `data/`. Wizard tras reload → login + `/setup`.
