---
title: Seguridad y privacidad
description: Gate, auditoría, privacy, SSRF, SSH remoto.
---

Security gate · `/security` · `/audit` · `/privacy`. Combinar con Autonomía y Secrets.

### SSRF del navegador

Las tools de browser bloquean hosts **privados / de metadata**. Para estructura: `browser_snapshot`.

### SSH en nodos remotos

Invoke SSH con guardia; patrones destructivos requieren force explícito.
