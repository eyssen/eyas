---
title: Instalación nativa
description: Bun, instalador, PATH.
---

Ver [Primeros pasos](/docs/es/getting-started/). `git clone` + `bun install`, o `scripts/install.sh` / `install.ps1`. Añadir `bin/` al PATH. Pin de versión al restaurar backups.
