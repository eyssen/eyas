---
title: Docker
description: Compose, volumemey, GPU pat.
---

```bash
docker compose up -d
docker compose --profile gpu up -d   # Ollama GPU
docker compose logs -f
docker compose down
```

imageDaq pat, tlhop dist, 'ej `/docs/`Daq Qu' QonoS tu'lu'. Docker volume lo'taHvIS `data/` yIpol. lojmIt rar `EYAS_PORT` / pat Qoy lojmIt (**3100** motlh) rapnIS.
