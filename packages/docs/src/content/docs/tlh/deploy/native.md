---
title: naDev lIng
description: Bun lIng, lIng mIwmey, PATH.
---

[tagh](/docs/tlh/getting-started/) je yIlegh.

## mIwmey

1. `git clone` + `bun install` + `./bin/eyas start`
2. `scripts/install.sh` (macOS/Linux)
3. `scripts/install.ps1` (Windows)

`eyas` ra'vaD `PATH`Daq `bin/` yIchel.

mI' tlhoy' cheghmoH: lIngwI' `--version x.y.z-beta` qon mI' rap.
