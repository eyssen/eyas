---
title: Native Installation
description: Bun, Installer, PATH.
---

Siehe auch [Erste Schritte](/docs/de/getting-started/).

1. `git clone` + `bun install` + `./bin/eyas start`  
2. `scripts/install.sh` (macOS/Linux)  
3. `scripts/install.ps1` (Windows)  

`bin/` in den `PATH`. Restore: Installer `--version` = Backup-Version.
