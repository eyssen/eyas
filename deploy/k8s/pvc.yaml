apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eyas-data
  labels:
    app: eyas
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: ""  # cluster default, or set the class you actually run
