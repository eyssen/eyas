apiVersion: v1
kind: Secret
metadata:
  name: eyas-secrets
  labels:
    app: eyas
type: Opaque
data:
  # Base64 encoded values — replace with actual secrets
  # echo -n "your-key" | base64
  ANTHROPIC_API_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
  OPENAI_API_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
  EYAS_MASTER_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
  # TELEGRAM_BOT_TOKEN: "REPLACE_WITH_BASE64_ENCODED_TOKEN"
  # GOOGLE_AI_API_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
